export const dynamic = "force-static"; // just to be safe

export default function LinkedInPage() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>LinkedIn integration is temporarily disabled</h1>
      <p>You can still use the rest of the site as normal.</p>
    </div>
  );
}
